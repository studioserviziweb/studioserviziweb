{% spaceless %}

{% set link = item.strip.link|default(empty) %}
{% set image = item.strip.image|default(empty) %}
{% set label = item.strip.label|default(empty) %}
{% set id_num = id_num %}
{% set tit = item.strip.title|default("800 595 493") %}
{% set numV = item.strip.content|default("800 595 493") %}

{% endspaceless %}<a href="tel:{{numV}}" class="strip-num-verde strip-pos{{id_num}}">
	<span class="verde">{{tit}}</span>
	<span class="num">{{numV}}</span>
</a>
